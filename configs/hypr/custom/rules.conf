# You can put custom rules here
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ######## Window rules ########

# Uncomment to apply global transparency to all windows:
# windowrule = opacity 0.89 override 0.89 override, match:class .*

# Disable blur for all xwayland apps
# windowrule = no_blur on, match:xwayland 1

# Throne - start on hidden special workspace (tray only)
windowrule = match:class ^(Throne)$, workspace special silent

# AyuGram/Telegram Mini Apps - обычное плавающее окно без эффектов compositor
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, float on
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, center on
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, size 480 640
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, opacity 1.0 override 1.0 override
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, no_blur on
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, no_shadow on
windowrule = match:title ^(Mini App:.*)$, match:class ^(com\.ayugram\.desktop)$, no_anim on

